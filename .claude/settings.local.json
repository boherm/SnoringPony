{
  "permissions": {
    "allow": [
      "Bash(grep:*)",
      "Bash(cmake --build build --target SnoringPony)",
      "Bash(cmake --build build)",
      "WebSearch",
      "Bash(cmake --build out/Debug --config Debug)"
    ]
  }
}
